//Crie um programa de contagem progressiva. 
//Peça um número e conte de 0 até esse número, usando um loop while no console do navegador.

let contMax = prompt ("Informe um número");
let contador = 0;

while ( contador <= contMax ) {
    console.log(contMax);
    contMax--;
}